import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  textView: {
    color: 'white',
    fontSize: 25,
    textAlign: 'center',
    marginVertical: '75%',
  },
  
});

export default styles;
